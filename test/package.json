{
  "private": true,
  "dependencies": {
    "playwright": "^1.19.0",
    "serve-handler": "^6.1.3"
  },
  "scripts": {
    "build": "openfl build html5 -final -Dplaywright",
    "test": "npm run build && node playwright-runner.js"
  }
}
